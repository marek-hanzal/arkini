import { EditorAssetTechnical } from "~/ui/resource/editor/EditorAssetTechnical";

export const EditorAssetTechnicalPage = ({ resourceId }: { readonly resourceId: string }) => (
	<EditorAssetTechnical resourceId={resourceId} />
);
