# 2026-07-06 — selector docs

- Added root `SELECTOR.MD` with the current selector model.
- Documented resolved runtime selectors, authoring item selector refs, item-vs-grant matching semantics, compiler normalization, validation behavior, and style rules.
- Linked selector docs from `README.md` and updated the short selector paragraph in `CONFIG.MD` so future passes do not need to re-read compiler/schema/runtime matching code just to know what `allOf` means.
