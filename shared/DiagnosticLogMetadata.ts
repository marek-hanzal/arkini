/** Support-ready rotating logs produced by the current diagnostic sink. */
export const DiagnosticLogFiles = {
	application: "support.md",
	lastGame: "last-game.json",
	session: "support.jsonl",
} as const;
