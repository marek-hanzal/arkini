import { settleItemDeliveryFx } from "~/engine/delivery/write/settleItemDeliveryFx";

/** Bridge-owned alias for the generation-guarded canonical delivery contact command. */
export const settleTileDeliveryFx = settleItemDeliveryFx;
