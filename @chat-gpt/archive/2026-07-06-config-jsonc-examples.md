# 2026-07-06 — CONFIG commented JSONC examples

- expanded `CONFIG.MD` with a dedicated commented `jsonc` examples section
- documented source fragment wrapper and `$schema` placement
- added commented examples for normal items, finite deposits, producer lines, capacity spend, output-owned effects, reusable craft inputs, random-board craft output, blueprint craft, grant source/effect consumption, and starting state
- corrected package artifact wording from old `.arkpack.gz` to current `.arkpack`
- updated era package wording to reflect folder-based era config split
