import LauncherHeroUrl from "../../../game/arkini/assets/hero.png?url";

/** Bundled Hero artwork shared by startup and out-of-game launcher screens. */
export const LauncherHeroAsset = {
	url: LauncherHeroUrl,
} as const;
