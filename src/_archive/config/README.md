# Config historical status

**Status:** Compiler/model superseded; feature archaeology only.

The canonical compiler, schemas, semantic diagnostics, resource validation, and packer are current and must not be redesigned from this directory.

Open historical config types only when a numbered feature task needs to understand old product semantics that are not already explicit in current authored game fragments.
