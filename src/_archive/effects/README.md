# Effects historical status

**Status:** Mostly superseded architecture; narrow oracle remains.

Current conditions, distance evaluation, line rules, and deterministic output rules replace the old effective-line and global grant interpretation.

The persistent active-effect save model is rejected. Consult this directory only for:

- product labels/read requirements in tasks 10 and 13;
- presentation/audio intent in tasks 14–15.

Temporary-item timing, expiry, retry, and deterministic output are superseded by the canonical v1 Tick lifecycle. The old expiry mutation and persistent active-effect save helpers were removed; Git retains them for archaeology.
