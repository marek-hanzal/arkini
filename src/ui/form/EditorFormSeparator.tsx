/** Separates sibling root objects without introducing another content surface. */
export const EditorFormSeparator = () => (
	<hr
		className="w-full border-0 border-t border-line-strong"
		data-ui="EditorFormSeparator"
	/>
);
