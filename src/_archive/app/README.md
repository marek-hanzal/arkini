# Application shell historical status

**Status:** Reference for task 12.

The old bootstrap and runtime wiring are rejected. Preserve only deliberate shell, splash, layout, reset, and navigation behavior when rebuilding the browser entrypoint over `GameSession`.
