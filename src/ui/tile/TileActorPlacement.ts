/** One measured Canvas-relative actor rectangle owned by a live slot anchor. */
export interface TileActorPlacement {
	readonly x: number;
	readonly y: number;
	readonly width: number;
	readonly height: number;
}
