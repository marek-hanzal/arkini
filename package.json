{
  "name": "arkini",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.2.17",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/arkini && vite dev",
    "build": "cd apps/arkini && vite build",
    "typecheck": "cd apps/arkini && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
